import { NoSsrDirective } from './no-ssr.directive';

describe('NoSsrDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSsrDirective();
    expect(directive).toBeTruthy();
  });
});
